@model IEnumerable<SISTEMEMPRESA.Models.SEGMODULO>

@{
    ViewBag.Title = "Modulo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="background-color:midnightblue">
    <h2 style=" text-align: center; color: whitesmoke; font-family: courier"> REGISTRO DE MODULOS</h2>
</div>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.detalle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.detalle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Modificar", "Edit", new { id=item.IdSegModulo }) |
            @Html.ActionLink("Detalle", "Details", new { id=item.IdSegModulo }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdSegModulo })
        </td>
    </tr>
}

</table>
